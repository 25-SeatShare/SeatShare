<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/home"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:gravity="center"
    android:orientation="vertical">

    <!-- 버튼 -->
    <ImageView
        android:id="@+id/logo_1"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginTop="120dp"
        android:layout_marginBottom="20dp"
        android:src="@drawable/logo_1" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="30dp"
        android:layout_gravity="center"
        android:layout_marginBottom="70dp"
        android:text="오늘도 안전한 지하철 이용 되세요!"
        android:textSize="17dp" />

    <!-- 좌석 등록 버튼-->
    <android.widget.Button
        android:id="@+id/btn_seat_registration"
        android:layout_width="263dp"
        android:layout_height="70dp"
        android:layout_marginBottom="15dp"
        android:background="@drawable/design_home_btn"
        android:text="좌석 등록"
        android:textSize="17dp" />

    <!-- 좌석 조회 버튼 -->
    <android.widget.Button
        android:id="@+id/btn_seat_check"
        android:layout_width="263dp"
        android:layout_height="70dp"
        android:layout_marginBottom="15dp"
        android:background="@drawable/design_home_btn"
        android:text="좌석 조회"
        android:textSize="17dp" />

    <!-- 하차 인증 버튼 -->
    <android.widget.Button
        android:id="@+id/btn_get_off"
        android:layout_width="263dp"
        android:layout_height="70dp"
        android:background="@drawable/design_home_btn"
        android:text="하차 인증"
        android:textSize="17dp" />

    <!-- 내비게이션 바 -->
    <View
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:layout_weight="1" />

    <LinearLayout
        android:id="@+id/home_nav_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="#3A83BF"
        android:clipToPadding="false"
        android:elevation="8dp"
        android:gravity="top"
        android:orientation="horizontal"
        android:padding="0dp"
        android:weightSum="3">

        <!-- 좌석 정보 버튼 -->
        <Button
            android:id="@+id/btn_seat_info"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@android:color/transparent"
            android:gravity="center"
            android:text="좌석 정보"
            android:textColor="#FFFFFF"
            android:textSize="14sp" />

        <!-- 포인트 버튼 -->
        <Button
            android:id="@+id/btn_point"
            android:layout_width="0dp"
            android:layout_height="80dp"
            android:layout_weight="1"
            android:background="@android:color/transparent"
            android:gravity="center"
            android:text="포인트"
            android:textColor="#FFFFFF"
            android:textSize="14sp" />

        <!-- 마이페이지 버튼 -->
        <Button
            android:id="@+id/btn_mypage"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:background="@android:color/transparent"
            android:gravity="center"
            android:text="마이페이지"
            android:textColor="#FFFFFF"
            android:textSize="14sp" />

    </LinearLayout>

</LinearLayout>


